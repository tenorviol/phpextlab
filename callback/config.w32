// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("callback", "for callback support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("callback", "enable callback support", "no");

if (PHP_CALLBACK != "no") {
	EXTENSION("callback", "callback.c");
}

